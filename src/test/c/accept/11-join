{
    "SELECT": ["employees.name", "departments.department_name"],
    "FROM": "employees",
    "JOIN": {
        "departments": {
            "ON": {
                "employees.department_id": "departments.id"
            }
        }
    }
}

